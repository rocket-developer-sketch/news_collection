server:
  port: 8082

spring:
  datasource:
#    url: jdbc:mysql://mysqldb:3306/news_api?serverTimezone=UTC&characterEncoding=UTF-8
    url: jdbc:mysql://mysqldb:3306/news_api?useUnicode=true&characterEncoding=UTF-8
    username: news_user
    password: ndb!user432Q
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
    open-in-view: false
cors:
  allowed-origins:
    - http://localhost:8083

jwt-conf:
  secret: news-data-jwt-secret-key-two-thousand-twenty-five-july
  access-token-validity: 1800000 # 30분
  refresh-token-validity: 3600000 # 1시간
  cookie:
    refresh-cookie-name: refresh_token
    domain: localhost
    secure: false
    same-site: Lax
    path: /

#    secure: true
#    same-site: None

batch:
  base-url: http://batch:8080/batch
  internal-secret: eiwoxc325kok07Acd

logging:
  level:
    root: INFO
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG